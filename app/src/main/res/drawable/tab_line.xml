<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when tab is not selected -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>